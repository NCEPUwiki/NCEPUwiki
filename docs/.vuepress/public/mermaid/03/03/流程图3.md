flowchart TB
    A(["开始：本地已有仓库（clone 完成）"])
    P1["① 更新：把别人的最新更改拉到本地<br/>git pull"]
    B["② 切换 / 创建自己的分支（可选）<br/>git switch -c 分支名"]
    C["③ 动手修改代码 / 文档"]
    D["④ 查看改动是否符合预期（可选）<br/>git status / git diff"]
    E["⑤ 暂存更改<br/>git add ."]
    F["⑥ 提交到本地仓库<br/>git commit -m '修改说明'"]
    G["⑦ 把修改推送到远程仓库<br/>git push<br/>（首次用 git push -u origin 分支名）"]
    Z(["完成：自己的修改已更新到远程"])

    A --> P1 --> B --> C --> D --> E --> F --> G --> Z

    classDef local fill:#E3F2FD,stroke:#1E88E5,color:#0D47A1
    classDef remote fill:#FFF3E0,stroke:#FB8C00,color:#E65100
    classDef conn fill:#EDE7F6,stroke:#5E35B1,color:#311B92
    classDef term fill:#E8F5E9,stroke:#43A047,color:#1B5E20
    class A,Z term
    class P1 remote
    class B,C,D,E,F local
    class G conn