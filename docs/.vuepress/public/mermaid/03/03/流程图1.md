
flowchart TB
    A(["开始：新项目，从 0 开始"])

    subgraph LOCAL["本地仓库（你的电脑）"]
        direction TB
        L1["创建项目文件夹并进入<br/>mkdir project </br> cd project"]
        L2["初始化仓库，生成 .git<br/>git init"]
        L3["添加文件并提交第一个版本<br/>git add .<br/>git commit -m 'first commit'"]
    end

    subgraph REMOTE["远程仓库（GitHub / Gitee）"]
        direction TB
        R1["登录代码托管平台"]
        R2["新建空仓库<br/>New repository </br>（不要勾选初始化文件）"]
        R3["③ 复制仓库地址<br/>HTTPS 或 SSH"]
    end

    subgraph CONNECT["连接两者"]
        direction TB
        C1["给本地仓库添加远程地址<br/>git remote add origin URL"]
        C2["首次推送并建立追踪关系<br/>git push -u origin main"]
    end

    A --> L1 --> L2 --> L3
    A --> R1 --> R2 --> R3
    L3 --> C1
    R3 --> C1
    C1 --> C2 --> Z(["完成：本地仓库与远程仓库已连接"])

    classDef local fill:#E3F2FD,stroke:#1E88E5,color:#0D47A1
    classDef remote fill:#FFF3E0,stroke:#FB8C00,color:#E65100
    classDef conn fill:#EDE7F6,stroke:#5E35B1,color:#311B92
    classDef term fill:#E8F5E9,stroke:#43A047,color:#1B5E20
    class A,Z term
    class L1,L2,L3 local
    class R1,R2,R3 remote
    class C1,C2 conn